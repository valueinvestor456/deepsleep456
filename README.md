# Deepsleep456 — เว็บไซต์ที่พัก

เว็บไซต์หน้าเดียว (static HTML/CSS/JS ไม่มี build tools) สำหรับที่พัก Deepsleep456
ตำบลโป่งแยง อำเภอแม่ริม จังหวัดเชียงใหม่

## วิธีเปิดดู

เปิด `index.html` ได้โดยตรง (ดับเบิลคลิก) หรือรันเซิร์ฟเวอร์ในเครื่อง:

```bash
python -m http.server 8080
```

แล้วเข้า `http://127.0.0.1:8080`

## โครงสร้างไฟล์

```
index.html         หน้าเว็บหลักทั้งหมด (8 ส่วนตามบรีฟ)
css/style.css       สไตล์ทั้งหมด
js/main.js          เมนูมือถือ, สไลด์ hero, ตัวกรองแกลเลอรี, lightbox
images/hero/        รูป hero (คืนกลางแจ้ง/มุมสูง)
images/rooms/       ห้องนอน 3 ห้อง
images/bathroom/    ห้องน้ำ + อ่างอาบน้ำ indoor/outdoor
images/common/      พื้นที่ส่วนกลาง (นั่งเล่น/ระเบียง)
images/exterior/    ภายนอกบ้าน/มุมสูง
images/og/          รูปสำหรับแชร์โซเชียล
images/logo.png     โลโก้จริงของ Deepsleep456 (พื้นหลังโปร่งใส)
```

ภาพทั้งหมดคัดและบีบอัดมาจาก `Deepsleep/Deepsleep webpage/` และ `Deepsleep/photoes deepsleep/`
(โฟลเดอร์ต้นฉบับที่มีรูปดิบกว่า 280 ไฟล์) ผ่าน ffmpeg เพื่อลดขนาดไฟล์ให้เว็บโหลดเร็ว

## ⚠️ รายการที่ต้องยืนยันก่อนขึ้นเว็บจริง (Pre-launch checklist)

ค้นหาคำว่า `data-confirm` ใน `index.html` เพื่อหาจุดที่ใช้ค่ายังไม่ยืนยัน:

- [ ] **ราคาเริ่มต้น** — ตอนนี้ใส่ไว้ที่ 1,000 บาท/คน/คืน (placeholder ตามบรีฟ)
- [ ] **เบอร์โทร/WhatsApp** — ตอนนี้ใส่ไว้ที่ 065-965-4326 (placeholder ตามบรีฟ)
- [ ] **ลิงก์ Facebook** — บรีฟระบุให้ใส่ในฟุตเตอร์ แต่ยังไม่ได้รับ URL จริง จึงยังไม่ได้ใส่ไอคอนนี้
- [x] **หมุด Google Maps** — อัปเดตเป็นพิกัดจริงแล้ว (18.8922564, 98.8279099) จากลิงก์ https://maps.app.goo.gl/nFLFRLGoEjABzDoX6 ซึ่งตรงกับหมุด "DEEPSLEEP456" บน Google Maps
- [ ] **โดเมนจริง** — ยังไม่ได้จดทะเบียน deepsleep456.com หรือชื่ออื่น (เช็คว่าว่างก่อน)
- [ ] **ตัวเลขอุณหภูมิเปรียบเทียบเชียงใหม่/โป่งแยง** (หัวข้อ "climate") — เป็นค่าประมาณจากทีมงาน ยังไม่มีแหล่งอ้างอิง/เครื่องวัดจริง ต้องยืนยันหรือปรับก่อนเผยแพร่
- [ ] รูปภาพยังไม่ผ่านการบีบอัดระดับโปรดักชัน (เช่น TinyPNG/Squoosh) เพียงลดขนาดเบื้องต้นด้วย ffmpeg เท่านั้น

## หมายเหตุ

- ไม่มีระบบจองออนไลน์ในระยะแรก ใช้ลิงก์ LINE/โทร/WhatsApp ตามบรีฟ
- เนื้อหารีวิวเขียนสรุปใหม่เอง ไม่ได้คัดลอกข้อความจาก Trip.com
- โครงสร้างภาษาไทยเป็นหลัก หากต้องการเพิ่มภาษาอังกฤษภายหลัง แนะนำให้คัดลอก `index.html`
  เป็นไฟล์ใหม่ (เช่น `en/index.html`) แล้วแปลข้อความ โดยใช้ `css/` และ `images/` ร่วมกันได้เลย
